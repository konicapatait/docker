application:
  name: SpringDockerGradleTemplate

logging:
  level:
    org.springframework: ERROR
    com.myspring.template: DEBUG

server:
  port : 8082

management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /actuator